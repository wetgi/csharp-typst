{
  "$schema": "https://json.schemastore.org/launchsettings.json",
  "profiles": {
    "http": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      // Pinned so the README can name a URL, and clear of the rendering
      // service's own 8080.
      "applicationUrl": "http://localhost:5080",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "TYPST_SERVICE_URL": "http://localhost:8080"
      }
    }
  }
}
