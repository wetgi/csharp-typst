<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Not published on its own: the client compiles RenderProtocol.cs in, the
         service references this project. Shared build settings come from
         Directory.Build.props. -->
    <TargetFrameworks>net10.0;netstandard2.0</TargetFrameworks>
    <RootNamespace>TypstRender.Contracts</RootNamespace>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

</Project>
